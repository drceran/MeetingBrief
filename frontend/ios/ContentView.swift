// MeetingNotesAI iOS App
// SwiftUI app for recording and summarizing meetings

import SwiftUI

@main
struct MeetingNotesAIApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct ContentView: View {
    var body: some View {
        Text("Meeting Notes AI")
            .font(.largeTitle)
            .padding()
    }
}