services:
  postgres:
    image: postgres:16
    container_name: meetingbrief-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: meetingbrief
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d meetingbrief"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
